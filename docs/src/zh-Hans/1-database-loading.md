# 数据库读取

CSManager 可以读取捆绑的 AMCSD 数据库，以及按需获取的 COD 数据库。

## 内置数据库

- `读取 AMCSD 数据库（含 ### 个晶体）` 读取随附的 AMCSD 数据集。
- `读取 COD 数据库（含 ### 个晶体）` 读取 COD 数据。首次使用时可能需要下载较大的 COD 压缩包。

## 自定义数据库

- `读取数据库` 打开已有的 CSManager 数据库文件。
- `保存数据库` 保存当前数据库。
- `清空数据库` 清除已读取的条目，但不会删除数据库文件本身。
